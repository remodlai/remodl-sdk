{
  "generate.predict": {
    "traces": [],
    "train": [],
    "demos": [
      {
        "augmented": true,
        "task_description": "You will receive a sentence. You will return whether the sentiment is positive or negative as a boolean. True means positive, False means negative.",
        "reasoning": "The task requires classifying a sentence's sentiment as positive (True) or negative (False). The input is a single sentence string, and the output is a boolean. Following DSPy conventions, we define an InputField for the sentence and an OutputField for the boolean result, with clear naming to indicate the positive sentiment mapping.",
        "signature_code": "class AnalyzeSentiment(dspy.Signature):\n    \"\"\"Classify sentence sentiment as positive (True) or negative (False).\"\"\"\n    \n    sentence: str = dspy.InputField()\n    is_positive: bool = dspy.OutputField()"
      },
      {
        "augmented": true,
        "task_description": "I need to summarize documents into brief summaries. You will receive a long document. You may receive a target word count (default 100 words). You must preserve key information. You can omit redundant details. You will return a concise summary.",
        "reasoning": "The task requires creating a DSPy Signature for document summarization. Inputs include the document and an optional target word count with a default of 100. The output is the concise summary. Following previous examples, we define InputFields for required/optional parameters with descriptive annotations, and an OutputField for the summary.",
        "signature_code": "class SummarizeDocument(dspy.Signature):\n    \"\"\"Summarize documents into brief summaries preserving key information.\"\"\"\n    \n    document: str = dspy.InputField()\n    target_word_count: int = dspy.InputField(desc=\"target word count (default 100)\")\n    summary: str = dspy.OutputField()"
      },
      {
        "augmented": true,
        "task_description": "You will receive a question and a list of context passages. You will return a short answer based on the context.",
        "reasoning": "The task requires generating a short answer from a question and context passages. Inputs include the question (string) and context passages (list of strings), both essential for deriving the answer. The output must be a concise response based solely on the provided context. Following DSPy conventions, the Signature defines these elements with clear type hints and descriptive field names.",
        "signature_code": "class AnswerQuestion(dspy.Signature):\n    \"\"\"Answer questions based on provided context passages.\"\"\"\n    \n    question: str = dspy.InputField()\n    context_passages: list[str] = dspy.InputField()\n    short_answer: str = dspy.OutputField()"
      },
      {
        "augmented": true,
        "task_description": "I need to translate text between languages. You will receive text in the source language. You will receive the target language code. You may receive a formality level (default neutral). You must maintain meaning accurately. You can adapt idioms appropriately. You will return the translated text.",
        "reasoning": "The task requires translating text between languages while considering formality levels. Inputs include source text, target language code, and an optional formality parameter (default neutral). The Signature must capture these inputs and produce translated text. Following DSPy conventions, we define input fields for text, target_language, and formality (with description noting default), and an output field for the translation result.",
        "signature_code": "class TranslateText(dspy.Signature):\n    \"\"\"Translate text between languages with specified formality level.\"\"\"\n    \n    text: str = dspy.InputField()\n    target_language: str = dspy.InputField()\n    formality: str = dspy.InputField(desc=\"formality level, default neutral\")\n    translated_text: str = dspy.OutputField()"
      },
      {
        "task_description": "You will receive a product description. You will receive a category taxonomy. You will return the most appropriate category. You will return a confidence score.",
        "signature_code": "class CategorizeProduct(dspy.Signature):\n    \"\"\"Categorize products into appropriate taxonomy categories.\"\"\"\n    \n    description: str = dspy.InputField()\n    taxonomy: list[str] = dspy.InputField()\n    category: str = dspy.OutputField()\n    confidence: float = dspy.OutputField()"
      },
      {
        "task_description": "I need to compare two documents for similarity. You will receive document A. You will receive document B. You will return a similarity score between 0 and 1. You will return key differences found.",
        "signature_code": "class CompareDocuments(dspy.Signature):\n    \"\"\"Compare two documents for similarity and identify differences.\"\"\"\n    \n    document_a: str = dspy.InputField()\n    document_b: str = dspy.InputField()\n    similarity: float = dspy.OutputField(desc=\"score between 0 and 1\")\n    differences: list[str] = dspy.OutputField()"
      },
      {
        "task_description": "I need to extract key entities from text. You will receive a text passage. You may receive entity types to focus on. You will return a list of entities found. You will return their types.",
        "signature_code": "class ExtractEntities(dspy.Signature):\n    \"\"\"Extract named entities from text passages.\"\"\"\n    \n    text: str = dspy.InputField()\n    entity_types: list[str] = dspy.InputField(desc=\"types to focus on\")\n    entities: list[str] = dspy.OutputField()\n    types: list[str] = dspy.OutputField()"
      },
      {
        "task_description": "I need to generate test cases for functions. You will receive a function signature. You will receive the function description. You must cover edge cases. You will return a list of test inputs. You will return expected outputs for each.",
        "signature_code": "class GenerateTests(dspy.Signature):\n    \"\"\"Generate comprehensive test cases for functions.\"\"\"\n    \n    signature: str = dspy.InputField()\n    description: str = dspy.InputField()\n    test_inputs: list[str] = dspy.OutputField()\n    expected_outputs: list[str] = dspy.OutputField()"
      }
    ],
    "signature": {
      "instructions": "Given the fields `task_description`, produce the fields `signature_code`.",
      "fields": [
        {
          "prefix": "Task Description:",
          "description": "Natural language task description using semantic keywords"
        },
        {
          "prefix": "Reasoning: Let's think step by step in order to",
          "description": "${reasoning}"
        },
        {
          "prefix": "Signature Code:",
          "description": "Valid Python code following DSPy format exactly"
        }
      ]
    },
    "lm": null
  },
  "metadata": {
    "dependency_versions": {
      "python": "3.12",
      "dspy": "3.0.3",
      "cloudpickle": "3.1"
    }
  }
}
